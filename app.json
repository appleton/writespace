{
  "name": "writespace",
  "scripts": {
  },
  "env": {
    "API_URL": {
      "required": true
    },
    "COUCH_URL": {
      "required": true
    },
    "EMBER_ENV": "production",
    "FORCE_HTTPS": "true"
  },
  "addons": [],
  "buildpacks": [
    {
      "url": "https://github.com/tonycoco/heroku-buildpack-ember-cli.git"
    }
  ],
  "environments": {
    "test": {
      "scripts": {
        "test-setup": "yarn add bower && bower install"
      },
      "env": {
        "API_URL": "/api",
        "COUCH_URL": "/db",
        "EMBER_ENV": "test"
      },
      "buildpacks": [
        { "url": "https://github.com/stomita/heroku-buildpack-phantomjs" },
        { "url": "https://github.com/heroku/heroku-buildpack-nodejs" }
      ]
    }
  }
}
